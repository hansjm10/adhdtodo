// ABOUTME: Hyperfocus mode screen for ADHD users - single task focus with timer
// Uses HyperfocusContainer for all timer logic and state management

import React from 'react';
import HyperfocusContainer from '../../src/components/HyperfocusContainer';

const HyperfocusScreen = () => {
  return <HyperfocusContainer />;
};

export default HyperfocusScreen;
