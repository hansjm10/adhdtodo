// ABOUTME: Focus mode selection screen - choose between Hyperfocus and Scattered modes
// Uses FocusModeContainer for all logic and state management

import React from 'react';
import FocusModeContainer from '../../src/components/FocusModeContainer';

const FocusModeScreen = () => {
  return <FocusModeContainer />;
};

export default FocusModeScreen;
