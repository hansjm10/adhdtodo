// ABOUTME: Scattered mode screen for rapid task switching with quick wins
// Uses ScatteredModeContainer for all logic and state management

import React from 'react';
import ScatteredModeContainer from '../../src/components/ScatteredModeContainer';

const ScatteredScreen = () => {
  return <ScatteredModeContainer />;
};

export default ScatteredScreen;
