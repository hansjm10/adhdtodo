// ABOUTME: Screen for creating new tasks with ADHD-friendly UI
// Uses CreateTaskContainer for all form logic and state management

import React from 'react';
import CreateTaskContainer from '../../src/components/CreateTaskContainer';

const CreateTaskScreen = () => {
  return <CreateTaskContainer />;
};

export default CreateTaskScreen;
